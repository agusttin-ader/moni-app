/** Re-export: mismas categorías que gastos fijos (compatibilidad). */
export {
  EXPENSE_CATEGORIES as DAILY_EXPENSE_CATEGORIES,
  expenseCategoryById as dailyCategoryById,
  expenseCategoryLabel as dailyCategoryLabel,
} from './expenseCategories.js'
